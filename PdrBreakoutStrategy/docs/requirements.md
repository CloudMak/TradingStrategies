Требования:
- dont use multiline statements
- dont change context variables from functions
- не объявлять типы внутри tuple-деструктуризации
- Все условия в if, while, тернарных операторах должны иметь тип bool.
- Переменные, инициализируемые значением na, должны объявляться с явным типом.
- должна использоваться версия 6 pine script
- omit the explicit return type in the function signature
- не разбивать тратегию на несколько файлов, весь код должен находится в strategy.pine кроме уже вынесенного модуля src/PrevDayLevels.pine